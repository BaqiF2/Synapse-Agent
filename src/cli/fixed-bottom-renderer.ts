/**
 * 固定底部渲染器 — 重导出层
 *
 * 已迁移到 renderer/bottom-bar.ts，此文件保持向后兼容。
 */

export {
  FixedBottomRenderer,
  FixedBottomRenderer as default,
} from './renderer/bottom-bar.ts';
export type {
  FixedBottomRendererOptions,
  FixedBottomState,
} from './renderer/bottom-bar.ts';
