/**
 * 文件功能说明：
 * - 该文件位于 `src/hooks/stop-hook-constants.ts`，主要负责 停止、Hook、常量 相关实现。
 * - 模块归属 Hook 领域，为上层流程提供可复用能力。
 *
 * 核心导出列表：
 * - `STOP_HOOK_MARKER`
 *
 * 作用说明：
 * - `STOP_HOOK_MARKER`：提供可复用的常量配置。
 */

export const STOP_HOOK_MARKER = '[StopHook]';
